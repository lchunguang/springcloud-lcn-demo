
import net.sourceforge.pinyin4j.PinyinHelper;
import net.sourceforge.pinyin4j.format.HanyuPinyinCaseType;
import net.sourceforge.pinyin4j.format.HanyuPinyinOutputFormat;
import net.sourceforge.pinyin4j.format.HanyuPinyinToneType;
import net.sourceforge.pinyin4j.format.HanyuPinyinVCharType;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class PingYinUtil {
    private final static transient Logger logger = LoggerFactory.getLogger(PingYinUtil.class);
    /**
     * 将字符串中的中文转化为拼音,其他字符不变
     *
     * @param inputString
     * @return
     */
    public static String getPingYin(String inputString) {
        StringBuffer output = new StringBuffer();
        try {
            HanyuPinyinOutputFormat format = new
                    HanyuPinyinOutputFormat();
            format.setCaseType(HanyuPinyinCaseType.LOWERCASE);
            format.setToneType(HanyuPinyinToneType.WITHOUT_TONE);
            format.setVCharType(HanyuPinyinVCharType.WITH_V);

            char[] input = inputString.trim().toCharArray();
            for (int i = 0; i < input.length; i++) {
                if (Character.toString(input[i]).
                        matches("[\\u4E00-\\u9FA5]+")) {
                    String[] temp = PinyinHelper.
                            toHanyuPinyinStringArray(input[i],
                                    format);
                    output.append((temp==null||temp.length==0)?"":temp[0]);
                } else {
                	output.append(Character.toString(input[i]));
                }
            }
        } catch (Exception e) {
            logger.error("转拼音错误"+inputString,e);
        }
        return output.toString();
    }

    public static void main(String[] args) {
        String aa = getPingYin("李春光弘佐");
        System.out.println(aa);
    }
}